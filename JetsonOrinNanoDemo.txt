python3 <<'PY'
import cv2

cap = cv2.VideoCapture("/dev/video0", cv2.CAP_V4L2)
if not cap.isOpened():
    raise SystemExit("カメラを開けません。デバイス番号を確認してください。")

cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)

try:
    while True:
        ok, frame = cap.read()
        if not ok:
            print("映像を取得できませんでした。")
            break

        cv2.imshow("USB Camera - Q to quit", frame)
        if cv2.waitKey(1) & 0xFF == ord("q"):
            break
finally:
    cap.release()
    cv2.destroyAllWindows()
PY


sudo apt install python3-venv python3-pip
python3 -m venv --system-site-packages ~/yolo-env
source ~/yolo-env/bin/activate
python -m pip install --upgrade pip
python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
python -m pip install ultralytics


python -c "import torch; print('PyTorch:', torch.__version__); print('CUDA:', torch.cuda.is_available())"


yolo predict model=yolo11n.pt source=0 device=0 imgsz=640 show=True


source ~/yolo-env/bin/activate
python -m pip install "numpy==1.26.4" "opencv-python==4.11.0.86"
python -m pip check




